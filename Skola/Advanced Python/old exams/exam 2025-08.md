## Question 1

1. `None` - `None`
2. `3` - `Number`
3. `TypeError` - `==` is not defined on set
4. `["", "2", "22", "222"]` - `list`
5. `{0, 1, 2}` - `set`
6. `false` - `boolean`
7. `None` - `None`
8. `11` - `number`
9. `2` - `number`
10. `63` - `number`
## Question 2

1. `
```python
{ line_nr for line_nr, stops in tramnetwork["lines"].items() if "Chalmers" in stops and "Korsvägen" not in stops }
```
2. 
```python
max([(time, a, b) for a, items in tramnetwork["times"].items() for b, time in items.items()])[1:]
```

## ## Question 3

1. `{ 3: {} }`
2. `{ 1: {2}, 2: {1}, 3: {} }`
3. 
```python
def add_edge(self, a, b):
	a, b = sorted((a, b))
	self.vertices.update({a, b})
	self.edges.add((a, b))
```
4. 
```python
def merge_graphs(self, other):
	self.vertices.update(other.vertices)
	self.edges.update(other.edges)
```

## Question 4

1. 
```python
mytree = Tree(1, [
	Tree(2, [atom(3), atom(4)]),
	Tree(5, [Tree(6, [atom(7)]), atom(8)])
])
```
2. 
```python
# Assumes the use of my own add_edge and merge_graphs methods
def tree2graph(tree):
	# convert a Tree as in Question 4 to a Graph as in Question 3
	
```
3. 